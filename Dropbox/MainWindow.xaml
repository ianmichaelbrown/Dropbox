<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="Dropbox.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Dropbox"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    xmlns:vms="using:Dropbox.ViewModels"
    Title="Dropbox">

    <StackPanel x:Name="root">
        <TextBlock>Input</TextBlock>
        <StackPanel Orientation="Horizontal">
            <TextBox MinWidth="500" Text="{Binding InputFolderPath}" />
            <Button Command="{Binding SelectInputFolderCommand}">Select folder</Button>
        </StackPanel>
        <TextBlock>Target</TextBlock>
        <StackPanel Orientation="Horizontal">
            <TextBox MinWidth="500" Text="{Binding TargetFolderPath}" />
            <Button Command="{Binding SelectTargetFolderCommand}">Select folder</Button>
        </StackPanel>
        <TextBox Text="(log messages)"></TextBox>
        <Button Content="{Binding SyncButtonText}" Command="{Binding SyncStateCommand}"></Button>
    </StackPanel>
</Window>
